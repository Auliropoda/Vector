1) Добавил функцию MyVector::shrink_fit()
- Она вызывается в функциях, которые удаляют элементы.
- если load_factor < 0.5, то мы устанавливаем capasity = size и перекидываем элементы.

2) Пересмотрел стратегию увеличения массива(capasity, а в частности функцию reserve).
- Если аддитивная, то делаем побитовый сдвиг.
- если мультипликативная, то умножаем на заданный coef.
Примечание: для конструкторов также учел два варианта.

